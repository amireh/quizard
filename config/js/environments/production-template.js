/* src/js/config/environments/production.js */
define([], function() {
  /**
   * @class Config
   *
   * Application-wide configuration.
   *
   * This object is exposed to app modules after initialization.
   */
  return {
    apiHost: '/api/v1',

    /**
     * The set of available locales, automatically generated by the locale
     * generation module.
     */
    availableLocales: [
      'en',
    ],

    localeNames: {
      'en': 'English',
    },

    defaultLocale: 'en',

    xhr: {
      timeout: 30000,
    },

    defaultPreferences: {
      user: {
      }
    }
  };
});

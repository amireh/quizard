/** @jsx React.DOM */
define([ 'react' ], function(React) {
  var Index = React.createClass({
    render: function() {
      return(
        <div>
          <header><h2>Quizard</h2></header>
        </div>
      );
    }
  });

  return Index;
});
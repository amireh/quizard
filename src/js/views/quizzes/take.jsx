/** @jsx React.DOM */
define(function(require) {
  var React = require('react');
  var TakeQuiz = React.createClass({
    render: function() {
      return(
        <div />
      );
    }
  });

  return TakeQuiz;
});
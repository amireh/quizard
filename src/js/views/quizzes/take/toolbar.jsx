/** @jsx React.DOM */
define(function(require) {
  var React = require('react');
  var Toolbar = React.createClass({
    render: function() {
      return(
        <div>
        </div>
      );
    }
  });

  return Toolbar;
});